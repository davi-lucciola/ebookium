# Application
APP_ENV=development

# Database (use postgres hostname when running via docker compose)
APP_DATABASE_URL=postgresql+psycopg://ebookium:ebookium@localhost:5432/ebookium

# Auth
APP_TOKEN_SECRET=dev-secret-change-me
